#include <iostream>

using namespace std;

int potencia
cout << fib(5) << endl;
cout << potencia(5, 3) << endl;

int potencia(int a, int b){
    if (b == 0){
        return 1;
    }
    else if (b > 0){
        return (a * potencia(a,b-1));
    }
}

int potencia(int base, int exp)
	if 
